# Stage 2 第 5 步交接：语义单元边界

完成日期：2026-09-16

## 本步完成

- `SHIFT+LEFT/RIGHT` 不再只移动一个 token。
- 连续数字会作为一个完整数字单元选择。
- 函数调用及其括号内容会作为一个完整单元选择。
- 普通括号表达式按匹配括号作为一个单元选择。
- 幂表达式的底数和指数会作为一个单元选择。
- 选区、DEL、替换和复制继续共用核心选区范围。
- 修复了选区完成后状态栏残留 `SHIFT` 的问题。

## 自动化验证

- CN CW 机器交互：133 项通过。
- CN CW 语义交互：54 项通过。
- 结构化模式：20 项通过。
- 说明书数学工具：37 项通过。
- 基础回归：169 项通过。
- APK 构建成功。

新增测试覆盖：

- 连续数字 `123`。
- 函数调用 `sin(2)`。
- 幂表达式 `3^2`。
- 选区 DEL。
- 选区替换。

## 当前边界

- 混合分数和更深层嵌套的完整结构选区还需要继续覆盖。
- 复杂嵌套表达式的视觉高亮还需要真机验证。
- Android 剪贴板菜单和多指/触感仍需要人工验收。

## GPT-5.6 Sol Medium / MuMu 验证记录

设备：MuMu，`127.0.0.1:7555`，`1260×2800`，480 dpi。

- `sin(2)`：通过。SHIFT+左键后整个函数调用（含括号）连续高亮。
- 幂表达式：通过。幂表达式整体高亮（当前截图输入为 `1^2`，语义边界行为正确）。
- 根号：通过。`√(2)` 整体高亮，根号及括号内容没有被拆开。
- 分数：首次验证误用了输入顺序（先按模板再输入分子），复核后按 `1` → `a/b` → `2` 输入，正确生成堆叠分数。
- 分数语义选区：已修复。第一次 SHIFT+左选中分母，第二次扩展到完整分子/分母；分隔符不再成为孤立选区。
- 分数复制：已改为导出 evaluator source，复制 `1/2` 而不是显示标签 `a/b`。
- 复制菜单：MuMu 长按可打开“复制选区 / 复制计算过程 / 复制计算结果 / 粘贴”菜单。

截图：

- `outputs/stage2-step5-sin-selection.png`
- `outputs/stage2-step5-power-selection.png`
- `outputs/stage2-step5-root-selection.png`
- `outputs/stage2-step5-fraction-selection.png`

## 下一步交接

推荐模型：`GPT-5.6 Sol Medium`

任务：复验本次分数修复，并继续做最终人工交互验收：

1. `a b/c` 生成分子、分母两个结构节点，而不是普通字符串。
2. SHIFT+左/右对分数整体、分子、分母按语义层级移动。
3. 复制选区后剪贴板只包含选区内容。
4. 复杂表达式替换和 DEL 不破坏括号、根号和幂。

分数结构通过后，再进入 Stage 2 最终人工触感、多指和剪贴板验收。
