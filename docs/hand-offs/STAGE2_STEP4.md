# Stage 2 第 4 步交接：语义选区核心

完成日期：2026-09-16

## 本步完成

- 核心状态机新增语义选区锚点和焦点。
- `SHIFT+LEFT` / `SHIFT+RIGHT` 可按 token 语义范围扩展选区。
- 普通左右移动会先折叠选区，再继续移动。
- DEL 在有选区时删除整个选区。
- 输入新按键时替换选区，而不是把字符插入选区中间。
- 核心状态向 Android 暴露 `hasSelection`、`selectionStart`、`selectionEnd`。
- 选区 token 在自然表达式树中带有选中标记，渲染层会显示高亮。
- 长按复制菜单在有选区时新增“复制选区”。
- 选区文本由核心语义 token 导出，不从屏幕绘制字符串反推。

## 验证结果

- CN CW 机器交互：131 项通过。
- CN CW 语义交互：54 项通过。
- 结构化模式：20 项通过。
- 说明书数学工具：37 项通过。
- 基础回归：169 项通过。
- APK 构建并安装到 MuMu：成功。

## 尚未完成

- 触摸屏上的 SHIFT 键和方向键组合尚未完整做真机差分。
- 滑动选择还没有把拖动范围直接映射成选区焦点。
- 分数、幂、根号等复合节点的整块高亮仍需真机检查。
- 选区复制菜单需要真机点击验证剪贴板内容。
- 选区模型目前以 token 范围为基础，后续要继续升级为完整语义路径。

## 下一步交接

推荐模型：`GPT-5.6 Sol Medium`

任务：使用已安装 APK 在 MuMu 上重新执行 Stage 2 真机差分验收，重点检查：

1. 滑动光标落点。
2. SHIFT+左右方向键选区高亮。
3. 选区复制、替换和 DEL。
4. 函数、括号、幂、分数等复杂表达式。
5. 多指、快速滑动和触感反馈。

如果真机发现选区边界或手势问题，再交给 `GPT-5.6 Sol High` 做下一轮交互修复。

