# Stage 2 接力状态

更新日期：2026-09-16

当前工作分支：`chat/continue-20260916`

关联 Draft PR：#2 `聊天模式续开发：计算边界、结果显示、自由选区与 UI 收尾`

原始 Stage 2 自动接力分支：`auto/stage2`

原则：不得静默改变既有功能语义；每一步完成后先跑回归测试并构建 APK，再写 hand-off。保持 AC、语义 DEL、历史、复制粘贴等既有逻辑不变。

## 当前状态

- Stage 2 Step 1：completed
- Stage 2 Step 2：completed
- Stage 2 Step 3：completed
- 当前阶段：Step 3 后真机差分验证 / 交互收尾
- 状态：device validation in progress

原 `STAGE2_STEP3.md` 的下一步为：进行真机差分验证，并在需要时单独安排后续交互优化。

## Step 3 后已完成的聊天模式收尾

以下内容已在 `chat/continue-20260916` 完成，不应重复开发：

1. EXE 仅自动补齐缺失的右括号；多余右括号、残缺运算符等非法表达式仍报错。
2. 结果显示取消省略号：优先缩小字号，极长纯数字可转科学计数法，最终保证字符不被 `...` 截断。
3. 自由选区改为手机式交互：
   - 长按进入选区后松手仍保留选区；
   - 左右边界可独立拖动；
   - 点击已选文字打开剪贴板菜单；
   - 函数、分数、幂等结构继续按语义单元吸附，不能被随意拆开。
4. 左侧 ON / Home / Settings / Back 四键整体放大并收紧成 2×2 控制块；方向键与右侧翻页键保持原尺寸。
5. 方向键不再写死横向中心，改为按左侧 2×2 控制块右边界与右侧翻页键左边界动态居中，使两侧留白尽量一致。
6. HOME 从硬 3×2 网格改为带间距的圆角应用卡片，图标更紧凑，选中态更克制。
7. 扩大自由选区左右抓手的实际触摸热区，视觉小点不变；短选区热区重叠时按距离判断左右边界。
8. 空表达式长按可直接打开粘贴入口，不再要求先选中数字。
9. 复制粘贴改为核心语义链：支持 `56²` / `56^2`、`√(9)`、`π/2`、`i^2`、`Ans+1`、`1E3`；未知字符整段拒绝，不能静默丢字符后改变数学含义。
10. 剪贴板交互补全触感：进入剪贴板菜单、执行复制/粘贴动作均有反馈。
11. CI 继续执行完整 `:core:check` 和 `:app:assembleCn991Debug`。

## 真机已确认项目

用户已在 vivo 真机确认：

- 左右选区边界均可拖动；
- 左右滑动可移动光标；
- 直接点击表达式可移动光标；
- 一根手指保持按下、另一根手指继续点击时没有明显阻碍或异常。

这些项目可视为已通过本轮基础人工验收，后续除非回归，不重复开发。

## 固定测试签名基线

旧问题：GitHub Actions 每次使用不同 debug 签名，导致同包名 APK 无法直接覆盖安装，vivo 会提示“软件包与现有软件包存在冲突 / 开发者签名异常”。

已完成：

- CI 缓存并恢复固定 `~/.android/debug.keystore`；
- 缓存首次缺失时主动初始化测试 keystore；
- `app/build.gradle` 的 debug signingConfig 明确绑定到该固定 keystore 路径；
- 连续在不同 GitHub Runner 上构建验证，证书 SHA-256 完全一致：

`8427d60083951dc5bceab9663c851070145182c66c11a467b1d72a734434c688`

- 固定签名起始基线：`0.3.9-debug`，`versionCode 323`；
- 基线 Release tag：`chat-20260916-stable-signing`；
- 基线 APK：`CN991-stable-update-0.3.9-debug.apk`。

迁移规则：从旧随机签名版本迁移到 0.3.9 时仍需最后卸载一次；安装 0.3.9 固定签名基线后，后续测试 APK 必须继续使用同一测试签名并递增 `versionCode`，不得重新生成或更换签名，否则再次失去覆盖更新能力。

## 0.3.10 真机反馈修复

用户在长整数平方真机测试中发现结果显示为 `3.086419753E35`，并反馈 Ans 不便复制。

已完成：

- 科学计数结果显示层不再因为宽度不足退回 `E` 记法；识别科学计数结果后固定绘制为“尾数 × 10 + 上标指数”的自然计算器形式；
- 自然科学计数显示会先逐步缩小字号，仍超宽时再压缩横向布局，保持 `×10` 上标结构；
- 修正核心直接输出 `×10^n` 时的指数边界解析，`×10^` 按完整 4 字符前缀处理；
- 剪贴板菜单在存在 Ans 时增加“复制 Ans”；
- 核心新增 Ans token 的触摸选中与剪贴板语义导出回归，确认选中 Ans 后导出内容仍为 `Ans`；
- 版本提升到 `0.3.10-debug` / `versionCode 324`；
- 正式 PR CI：run `35087931447`，完整核心回归通过，其中 CN CW machine checks 为 171 项；CN991 Debug APK 构建与 artifact 上传通过；
- 0.3.10 APK 证书 SHA-256 仍为固定签名：

`8427d60083951dc5bceab9663c851070145182c66c11a467b1d72a734434c688`

- 0.3.10 Release tag：`chat-20260916-natural-sci-ans-0310`；
- APK：`CN991-0.3.10-debug.apk`。

## 下一步：继续真机差分验收

当前不要先加新功能，先验证 0.3.10。本轮最优先的三项是：

1. **不要卸载 0.3.9**，直接安装 0.3.10，确认可以覆盖更新且应用数据不被清除；这是固定测试签名方案的最终真机验收。
2. 重现长整数平方等大数结果，确认显示为类似 `3.086419753 × 10³⁵` 的自然形式，不再出现 `E35`。
3. 验证 Ans 复制：已有 Ans 后长按/打开剪贴板菜单，确认“复制 Ans”可用；同时可尝试选中表达式中的 Ans 后复制、粘贴并继续计算。

以上通过后，再继续检查尚未明确人工确认的项目：

- 扩大后的选区抓手命中范围是否明显更容易抓取；
- 空表达式直接长按粘贴是否稳定；
- 分数、根号、函数、幂等复杂结构拖选/DEL 是否保持完整语义；
- EXE 缺右括号自动补齐以及非法表达式仍报错；
- 超长结果整体观感是否可接受；
- ON / Home / Settings / Back 四键、方向键两侧留白是否合适；
- HOME 卡片间距、图标、标题和选中态是否合适。

## 验收后的处理

- 若以上全部通过：更新本 hand-off 为 `device validation passed`，确认 Draft PR #2 无新增失败检查，再由用户决定是否合并到 `main`。
- 若有任一项失败：只针对失败项在 `chat/continue-20260916` 修复，保持其他已通过语义不变；修复后重新运行完整 `:core:check`、CN991 Debug APK 构建和对应真机复验。
- 不在未完成真机验收前直接合并 `main`。
