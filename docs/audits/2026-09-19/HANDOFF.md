# 审查后的交接待办

被审查提交：edb97fd1936b4c9ddc01d1a09773a63de5ba1646。
本次提交只归档审查材料；下面各项仍未修复。详细复现见 REVIEW.md；编号可用于后续修复提交和回归测试名称。

## 架构/规范轴

- [ ] A1：普通表达式 OK/ENTER 的计算调度不得阻塞 UI。
- [ ] A2：数值循环响应取消；AC 后新任务可及时开始。
- [ ] A3：粘贴、触摸光标/选区、格选择统一失效旧任务。
- [ ] A4：显示区与数字键交叉释放时，按 pointerId 正确结束手势和长按。

## 功能/结果轴

- [ ] B1：Base-N 按当前进制校验，HEX 1E 可转换为 DEC 30。
- [ ] B2：矩阵/向量非零小量不被格式化为 0。
- [ ] B3：工作流错误恢复按格加载，内部求值串不能写回单格。
- [ ] B4：HOME 幂等，重复 HOME 不绕过应用切换清理。
- [ ] B5：无实根不等式返回正确全实数/空集。
- [ ] B6：三/四次重根可靠求解；不收敛不得伪装成功。
- [ ] B7：复数 log 使用底数 10，与普通路径一致。
- [ ] B8：零底数幂正确；NaN/Infinity 不进入 RESULT/Ans。
- [ ] B9：方差和回归采用稳定算法，覆盖大偏移及带频数数据。
- [ ] B10：FORMAT 作用于当前查看结果，历史与最新 Ans 解耦。
- [ ] B11：复数验证修补正式进入源码和永久回归，未实现入口不假装可用。

## 数值附录后续项

- [ ] 极小系数/根的缩放不变性与绝对截断。
- [ ] 标量/复数隐式乘法优先级一致性。
- [ ] 复平方根小分量的稳定计算。
- [ ] 非零极小虚部的显示。
- [ ] 负大数 asinh 的稳定计算。
- [ ] 精确三角零点参与后续数值计算的处理。

## 继续方式

建议先修算错和数据损坏，再统一任务/状态边界，之后补入口与生命周期，并把本次反例纳入永久回归。避免一次性重写整个状态机；每一轮要保留旧行为并记录新旧差分。

修复后执行核心回归、Debug/lint、对应 Release 签名门禁，再集中验证受改动影响的设备交互。多指释放和慢计算期间粘贴尚需设备实际复现。

此前 artifact 10495340801 已通过的函数表分页、结果页隔离和覆盖安装无需无故重复；它并未验收后来新增的矩阵记忆、频数统计或 Base-N 表单。

同步更新 README、FEATURE_COVERAGE、ARCHITECTURE、Stage 6 relay 和验收包对应提交，避免后续接手者把脚本已提交、CI 通过或旧 APK 验收误当成最新全部功能已完成。

PR #9 继续保持 Draft；所有修复和集中验收完成后，仍须用户明确授权才能合并。
