name: 临时 Stage6 工作流校验接入

on:
  push:
    branches: [ stage6/workflow-experience ]
    paths:
      - '.github/workflows/temp-stage6-validation.yml'

permissions:
  contents: write

jobs:
  patch-verify-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: 检出分支
        uses: actions/checkout@v4
        with:
          ref: stage6/workflow-experience
          fetch-depth: 0

      - name: 配置 Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: 配置 Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: 应用 Stage 6 校验补丁
        run: python scripts/temp_stage6_validation_patch.py

      - name: 添加 Gradle 执行权限
        run: chmod +x ./gradlew

      - name: 完整核心回归
        run: ./gradlew :core:check

      - name: Android Debug 构建
        run: ./gradlew :app:assembleCn991Debug

      - name: 提交正式产品改动
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add core/src/main/java/com/codex/fx991/core/cw/CnCwWorkflowSession.java \
                  core/src/main/java/com/codex/fx991/core/cw/CnCwMachine.java
          git commit -m "feat(stage6): validate workflow cells before execution"
          git push origin HEAD:stage6/workflow-experience
