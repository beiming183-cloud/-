package com.codex.fx991.core;

/** Physical-key intents. The UI never edits expression text directly. */
public enum CalculatorKey {
    SHIFT, ALPHA, MODE, SETUP, ON,
    LEFT, RIGHT,
    RECIPROCAL, SQUARE, SQRT, POWER, LOG, LN,
    NEGATE, DMS, HYP, SIN, COS, TAN,
    RCL, ENG, OPEN, CLOSE, FRACTION, M_PLUS,
    DIGIT_0, DIGIT_1, DIGIT_2, DIGIT_3, DIGIT_4,
    DIGIT_5, DIGIT_6, DIGIT_7, DIGIT_8, DIGIT_9,
    DOT, EXP, ANS, PI,
    ADD, SUBTRACT, MULTIPLY, DIVIDE,
    DEL, AC, S_D, EQUALS, ANGLE
}
